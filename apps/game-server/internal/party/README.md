# internal/party

Party / group system — implements spec §38. TBD.

Will host party invite / accept / leave flows, shared world position,
// multi-pet combat, and the wire namespace `party.*` declared in spec §50.