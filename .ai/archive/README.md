# `.ai/archive/` — superseded docs (historical only)

Anything moved out of `plan/`, `decisions/`, `design/`, `reviews/`, or
`handoffs/` lives here. **Never delete** `.ai/` files; always move to
`archive/` and commit the move so history is preserved.

**Archived material is not part of cold-start context.** Treat it as
historical reference only.

## Convention

- Keep the original filename; add a leading `YYYY-MM-DD-` if not already
  present (helps chronological sort).
- Add a one-line header at the top of the moved file pointing to its
  replacement (if any):
  ```
  <!-- Moved to: <relative path> on YYYY-MM-DD. Reason: <short reason>. -->
  ```
- Index at the bottom of this README.

## Index

| Original path                                   | Moved date  | Replacement path                         |
| ----------------------------------------------- | ----------- | ---------------------------------------- |
| _none yet_                                      |             |                                          |
