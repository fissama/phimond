# Phimond Implementation Plan

**Goal:** Replace the architecture incrementally with a playable, persistent pet-lineage reconstruction.
**Spec:** User project update, retained in docs/PROJECT_REQUIREMENTS.md.
**Architecture:** Go/MySQL authoritative modular monolith, Godot client, Next.js companion, shared JSON content and versioned intent/event protocol.
**Constraints:** Preserve legacy files/saves/server. New database only. No real money. No invented historical facts. Work directly here: no .git exists, so worktrees/commits are unavailable. User has authorized the architecture and instructed continuation without re-asking settled decisions.

- [x] Domain/content: table-driven definitions, validated links, deterministic RNG, pet/breeding/capture/battle rules. Tests first for inheritance from learned skills, replay, status effects, invalid commands and distributions.
- [x] Persistence: isolated bootstrap, migrations with checksums, authentication, locked character mutations with request deduplication and audit/lineage projections. MySQL integration test for restart and concurrent commands.
- [x] Transport: REST content/account/lineage and authenticated WebSocket intents; validation, bounded input and origin policy; test end-to-end.
- [x] Godot: login, horizontal room exploration, NPC/ranch interaction, battle controls, pet management and ancestry. Headless parse and connection smoke test.
- [x] Next.js: encyclopedia, recipe dependency planner, account pets and lineage, research confidence. Build/typecheck and browser inspection.
- [x] Local run/docs: .env example, independent Compose file, new schema, smoke run, test/race suite, honest feature/backlog report.

Contract: packages/protocol/README.md. Each implementer owns separate directories. Review implementation and integration before declaring completion.

## Verification ledger
Database created in new dedicated schema only. Go tests and remote MySQL tests passed. Godot and companion live paths passed. No Git commit/worktree was created because the supplied folder has no repository metadata. Later MMORPG features are explicitly outside this first delivery, tracked in docs/STATUS.md.
